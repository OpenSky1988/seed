import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  image: {
    borderRadius: 12,
    marginTop: 40,
    width: '100%',
    height: undefined,
    aspectRatio: 1,
  },
});

export default styles;
